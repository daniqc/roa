<%= form_for(@resource_file) do |f| %>
  <% if @resource_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@resource_file.errors.count, "error") %> prohibited this resource_file from being saved:</h2>

      <ul>
      <% @resource_file.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :resource_id %><br />
    <%= f.number_field :resource_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
