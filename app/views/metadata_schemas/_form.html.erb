<%= form_for(@metadata_schema, :remote => true) do |f| %>
  <% if @metadata_schema.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@metadata_schema.errors.count, "error") %> prohibited this metadata_schema from being saved:</h2>

      <ul>
      <% @metadata_schema.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <%= f.hidden_field :parent_id %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit :class => "btn btn-success"%>
  </div>
<% end %>
